<%if @user_check.name != "" && @user_check.name != nil%>
   <span class="sr-only">Reticulating splines: 0%</span>
   <br />
   <p>Reticulating splines</p>
<div class="progress">

  <div id="bar1" class="progress-bar progress-bar-success" role="progressbar" style="width: 0%">
  </div>
  </div>

  <p>Reticulating splines</p>
  <div class="progress">

  <div id="bar2" class="progress-bar progress-bar-info" role="progressbar" style="width: 0%">
  </div>
</div>



  <div id="result">
  <%if @user_check.score < 50 %>
    <h2>Unlikely.</h2>
    <p>
    There is a less than
  <% else %>
    <h2>Probably.</h2>
    <p>
    There is a 
  <% end %>
  <%= @user_check.score %>% chance that <a href="http://www.twitter.com/<%= @user_check.name %>">@<%= @user_check.name %></a> is <a href="http://www.twitter.com/edjeff/">@edjeff</a>.</p>
  </div>
<% end %>